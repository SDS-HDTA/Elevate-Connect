name: 'Build and deploy to GCP'

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy:
    name: 'Deploy to GCP for prod'
    if: ${{ github.ref == 'refs/heads/main' || github.base_ref == 'main' }}
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deployment.yml
    with:
      ref: ${{ github.sha }}
    secrets: inherit
