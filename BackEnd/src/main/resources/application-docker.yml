spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/elevate_connect
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  cloud:
    gcp:
      core:
        enabled: false
      sql:
        enabled: false
